{
  "type": "mysql",
  "host": "${MYSQL_HOST}",
  "port": "${MYSQL_PORT}",
"username": "${MYSQL_USER}",
"password": "${MYSQL_PASSWORD}",
"database": "${MYSQL_DATABASE}",
"entities": ["dist/**/*.entity{.ts,.js}"],
"synchronize": true
}